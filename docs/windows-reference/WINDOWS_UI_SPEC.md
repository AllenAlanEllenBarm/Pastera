# Windows V1 界面与交互规格

## 设计目标

Windows 客户端保持 Pastera 的高频、键盘优先和低打扰特性。界面应像 Windows
11 原生效率工具，而不是 macOS 界面的皮肤复制：使用 WinUI 3 的系统字体、
主题、焦点环、Flyout、NavigationView、ContentDialog、TeachingTip、
InfoBar 和原生窗口行为。

## 全局结构

### 托盘与主面板

- 通知区域图标左键打开主面板，右键打开应用菜单。
- 主面板为单实例无任务栏工具浮层；失焦关闭，但打开编辑器、文件选择器或
  子面板时不得误关。
- 顶部保留内容类型筛选和分页；底部保留搜索、历史、Snippet、密码箱、同步
  状态和设置入口。
- 默认窗口建议宽 360 到 420 epx、高 520 到 680 epx；屏幕边缘自动翻转，
  不得越出工作区。
- 列表行保持紧凑，主点击区执行复制/粘贴，次级操作通过悬停按钮、右键菜单
  或键盘命令提供。

### 独立工作窗口

- 设置、历史搜索、Snippet 编辑器和脚本编辑器使用正常可调整窗口，并参与
  Alt+Tab；托盘浮层不参与 Alt+Tab。
- 窗口位置和尺寸可恢复，但必须校正到当前显示器工作区。
- 危险操作使用 ContentDialog，并明确对象、影响范围和不可逆性。

## 页面规格

### 历史

- 过滤类型至少覆盖全部、文本、图片、文档、压缩包、代码、PDF、其他。
- 搜索支持普通文本、Regex、大小写敏感；空查询显示最近历史。
- 行内显示类型、摘要、时间和必要预览；完整内容放在详情/预览区，不让长文本
  撑高列表。
- 编辑必须是显式动作，保存和取消清楚；删除后刷新筛选、分页和焦点位置。
- 图片、文件和密码类内容不得在通知、日志或无意悬停中泄露完整内容。

### Snippet

- 浏览态和编辑态分离。浏览态用于搜索、选择和粘贴；编辑态提供文件夹树、
  条目详情、排序、移动、启停、导入与导出。
- 左侧树最小宽度 220 epx；右侧详情在小窗口下改为单列导航，不压缩成不可读
  的双栏。
- 空状态必须说明下一步，并只突出一个主动作。
- 导入先预览数量和冲突策略；导出明确文件位置与成功/失败结果。

### 密码箱

- 未创建：创建主密码并二次确认；提示忘记主密码无法恢复。
- 已锁定：主密码解锁为标准路径；Windows Hello 可在已经成功解锁并明确启用后
  作为便利入口，不能取代 KDBX 主密码。
- 已解锁：文件夹、条目列表和详情分区；密码默认隐藏，复制后提供短时反馈，
  自动锁定状态可见。
- 认证失败使用就地错误，不清空用户仍需修正的输入；不得记录主密码、明文密码
  或完整用户名。

### 脚本

- 列表展示名称、启用状态和触发时机；编辑器使用等宽字体、行号和明确保存状态。
- 测试区输入与输出分开，错误包含安全的行列信息。
- UI 只能表达受限的 `transform(clip)` 能力，不能提供文件系统、网络、进程或
  系统访问的暗示。

### 设置

- 使用左侧分类导航：常规、历史、脚本、快捷键、忽略应用、同步、关于。
- Windows 替换规则：登录时打开使用 StartupTask/启动设置；自动粘贴说明改为
  Windows 输入注入与权限限制；Finder 改为文件资源管理器；Command 快捷键改为
  Ctrl/Win/Alt；Sparkle 改为 Windows 更新通道。
- OneDrive 只选择本地 OneDrive 内可写目录，不出现 Microsoft 登录或 OAuth。
- 破坏性操作单独分组，不与普通开关混排。

## 状态与反馈

每个页面至少实现：加载中、空、正常、部分不可用、操作中、成功、可恢复错误和
不可恢复错误。后台同步或更新使用非阻塞状态；需要用户处理时使用 InfoBar，
不能连续弹出模态框。

## 可访问性和键盘

- 所有动作均可通过键盘到达；Tab 顺序遵循视觉顺序，Esc 关闭浮层或取消编辑，
  Enter 执行当前主动作。
- 可见焦点环不得被自定义背景覆盖；图标按钮必须有 AutomationProperties.Name。
- 支持系统浅色、深色、高对比度、文本缩放和 100%/125%/150% DPI。
- 不只依赖颜色表达选择、错误、同步或锁定状态。

## 截图验收矩阵

| 场景 | macOS 基线 | Windows 11 证据 | 状态 |
| --- | --- | --- | --- |
| 主面板历史与类型筛选 | 已有 | 待补 | 待实现 |
| 主面板搜索 | 已有 | 待补 | 待实现 |
| 历史独立搜索与结果 | 空状态已有 | 待补 | 待实现 |
| Snippet 浏览与编辑 | 空状态已有 | 待补 | 待实现 |
| 密码箱创建/锁定/解锁 | 锁定态已有 | 待补 | 待实现 |
| 脚本列表/编辑/测试 | 空状态已有 | 待补 | 待实现 |
| 设置全部分类 | 已有 | 待补 | 待实现 |
| OneDrive 正常/同步中/失败 | 正常态已有 | 待补 | 待实现 |
| 首次启动与权限引导 | 代码契约已有 | 待补 | 待实现 |
| 安装/升级/卸载 | 无通用视觉基线 | 待补 | 待实现 |

Windows 截图统一使用合成数据、Windows 11 默认字体和相同主题；每个主要窗口
至少补充浅色、深色和 125% 缩放三组证据。任何截图不得包含真实密码、token、
OneDrive 绝对路径、私有剪贴板内容或个人账号信息。
